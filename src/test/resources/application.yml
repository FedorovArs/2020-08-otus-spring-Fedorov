spring:
  datasource:
    url: jdbc:h2:mem:testdb
    schema: schema.sql
    data: data-test.sql
    initialization-mode: always

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
